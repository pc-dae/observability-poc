apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: kubernetes-cluster-dashboard
    files:
      - kubernetes-cluster.json
      - otel-host-metrics.json
      - victoria-metrics-overview.json
      - loki-ingestion.json

commonLabels:
  grafana_dashboard: "1"

namespace: grafana
